import { TogglePassword } from './toggle-password';

describe('TogglePassword', () => {
  it('should create an instance', () => {
    const directive = new TogglePassword();
    expect(directive).toBeTruthy();
  });
});
